(ns {{namespace}}.service.example-test
  (:require [clojure.test :refer :all]
            [integrant.core :as ig]
            [{{namespace}}.service.example :as example]))

(deftest service-test
  ;; FIXME
  )
